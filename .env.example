# Database Configuration
# ====================

# Database Driver
# Options:
#   - sqlite3: Use SQLite database only (default, for development)
#   - postgres: Use PostgreSQL database only (for production)
#   - dual: Use BOTH PostgreSQL and SQLite simultaneously (recommended for backup)
DB_DRIVER=sqlite3

# ========================================
# Single Database Mode (sqlite3 or postgres)
# ========================================

# Database Connection String (DSN)
# For SQLite (default):
# DB_DSN=./ritel.db

# For PostgreSQL (production):
# DB_DSN=host=localhost port=5432 user=ritel password=your_password dbname=ritel_db sslmode=disable

# Example PostgreSQL configurations:
# Local development:
# DB_DSN=host=localhost port=5432 user=ritel password=ritel123 dbname=ritel_db sslmode=disable

# Remote server:
# DB_DSN=host=192.168.1.100 port=5432 user=ritel password=secure_password dbname=ritel_db sslmode=require

# Cloud (with SSL):
# DB_DSN=host=db.example.com port=5432 user=ritel password=secure_password dbname=ritel_db sslmode=require

# ========================================
# Dual Database Mode (postgres + sqlite3)
# ========================================
# When DB_DRIVER=dual, data is automatically saved to BOTH databases
# PostgreSQL is used as primary (for reads), SQLite as backup

# PostgreSQL Connection (Primary Database)
# DB_POSTGRES_DSN=host=localhost port=5432 user=postgres password=postgres dbname=ritel_db sslmode=disable

# SQLite Connection (Backup Database)
# DB_SQLITE_DSN=./ritel.db

# Benefits of Dual Mode:
# - Automatic backup to SQLite while using PostgreSQL
# - Data redundancy - if one database fails, you have the other
# - Easy migration between databases
# - PostgreSQL for performance, SQLite for local backup

# PostgreSQL DSN Parameters:
# - host: Database server hostname or IP
# - port: Database server port (default: 5432)
# - user: Database username
# - password: Database password
# - dbname: Database name
# - sslmode: SSL mode (disable, require, verify-ca, verify-full)

# Instructions:
# 1. Copy this file to .env
# 2. Update the values according to your setup
# 3. Make sure .env is in .gitignore (do not commit sensitive data!)

# ========================================
# Web Server Configuration
# ========================================
# Enable web server mode (HTTP/REST API)
# Set to true to enable web access alongside desktop app
# Set to false to run desktop-only mode (default)
WEB_ENABLED=false

# Web server port
# Default: 8080
# The port where the HTTP/REST API will be accessible
WEB_PORT=8080

# Web server host
# Default: 0.0.0.0 (listen on all interfaces)
# Use 127.0.0.1 for localhost-only access
WEB_HOST=0.0.0.0

# ========================================
# JWT Authentication Configuration
# ========================================
# JWT Secret Key for token signing
# IMPORTANT: Change this to a strong, random secret in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=change-this-to-strong-secret-in-production

# JWT Token Expiry (in hours)
# Default: 24 hours
# After this time, users need to re-login
JWT_EXPIRY_HOURS=24

# ========================================
# CORS Configuration
# ========================================
# Allowed origins for Cross-Origin requests
# Separate multiple origins with commas
# Use * to allow all origins (NOT recommended in production)
# Examples:
#   Development: http://localhost:5173,http://localhost:3000
#   Production: https://yourapp.com,https://www.yourapp.com
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Allow credentials (cookies, authorization headers) in CORS requests
# Set to true if your frontend needs to send credentials
CORS_ALLOW_CREDENTIALS=true
