# ========================================
# SYNC MODE CONFIGURATION
# Offline-First with Auto-Sync
# ========================================
# This mode enables desktop app to work offline with local SQLite,
# and automatically sync to remote PostgreSQL server when online.
#
# Features:
# - ✅ Works offline (no internet required)
# - ✅ Auto-sync when connection is available
# - ✅ Queues operations during offline
# - ✅ Real-time sync monitoring
# ========================================

# Enable Sync Mode
SYNC_MODE=enabled

# Local Database (SQLite) - Always used for fast local operations
SYNC_SQLITE_DSN=./ritel.db

# Remote Database (PostgreSQL) - Synced when online
# IMPORTANT: Change this to your actual server address
SYNC_POSTGRES_DSN=host=localhost port=5432 user=ritel password=ritel123 dbname=ritel_db sslmode=disable

# For cloud deployment, use server IP/domain:
# SYNC_POSTGRES_DSN=host=server.example.com port=5432 user=ritel password=your_password dbname=ritel_db sslmode=disable

# ========================================
# Web Server Configuration
# ========================================
# Enable web server for remote access (optional)
WEB_ENABLED=true
WEB_PORT=8080
WEB_HOST=0.0.0.0

# ========================================
# JWT Authentication Configuration
# ========================================
JWT_SECRET=ritel-app-secret-key-change-this-in-production-2024
JWT_EXPIRY_HOURS=24

# ========================================
# CORS Configuration
# ========================================
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
CORS_ALLOW_CREDENTIALS=true

# ========================================
# HOW TO USE SYNC MODE
# ========================================
# 1. Copy this file to .env: cp .env.sync .env
# 2. Update SYNC_POSTGRES_DSN with your server address
# 3. Restart the application
# 4. App will work offline and auto-sync when online!
#
# Monitoring Sync Status:
# - API: GET http://localhost:8080/api/sync/status
# - API: POST http://localhost:8080/api/sync/force (manual sync)
# - API: GET http://localhost:8080/api/sync/pending (view queue)
# ========================================
